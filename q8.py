# Write a Python program to calculate the power of a number using ** operator
def demo(base,exponent):
  return base**exponent

base=int(input("enter the number: "))
exponent=float(input("enter the number:"))
print("power is ",demo(base,exponent))
    
